{
  "name": "mfe",
  "private": true,
  "workspaces": {
    "packages": [
      "main",
      "todo"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "install": "lerna run install",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^8.1.3"
  }
}
